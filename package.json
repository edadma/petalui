{
  "name": "petalui",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter docs dev",
    "test": "pnpm -r test",
    "publish": "pnpm --filter @edadma/petalui publish --access public"
  }
}
