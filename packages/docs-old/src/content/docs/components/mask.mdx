---
title: Mask
description: Apply CSS masks to crop content into various shapes.
tableOfContents: false
---

import {
  BasicShapesDemo,
  AllShapesDemo,
  HalfMasksDemo,
} from '../../../components/MaskDemo'
import MaskDemoSource from '../../../components/MaskDemo.tsx?raw'
import Example from '../../../components/Example.astro'

Apply CSS masks to crop content into various shapes.

## Import

```tsx
import { Mask } from 'asterui'
```

## Examples

<div class="examples">

<Example title="Basic Shapes" description="Apply different mask shapes to images." source={MaskDemoSource} fn="BasicShapesDemo">
  <BasicShapesDemo client:load slot="demo" />
</Example>

<Example title="All Shapes" description="All 15 available mask shapes." source={MaskDemoSource} fn="AllShapesDemo">
  <AllShapesDemo client:load slot="demo" />
</Example>

<Example title="Half Masks" description="Show only half of the masked content." source={MaskDemoSource} fn="HalfMasksDemo">
  <HalfMasksDemo client:load slot="demo" />
</Example>

</div>

## API

### `Mask`

| Property | Description | Type | Default |
|----------|-------------|------|---------|
| `shape` | Shape of the mask | `'squircle' \| 'heart' \| 'hexagon' \| 'hexagon-2' \| 'decagon' \| 'pentagon' \| 'diamond' \| 'square' \| 'circle' \| 'star' \| 'star-2' \| 'triangle' \| 'triangle-2' \| 'triangle-3' \| 'triangle-4'` | - |
| `half` | Show only half of the mask | `'half-1' \| 'half-2'` | - |
| `children` | Content to mask (typically an image) | `React.ReactNode` | - |
| `className` | Additional CSS classes | `string` | - |

## Accessibility

- Uses semantic div element
- Ensure masked images have appropriate alt text
- Passes through HTML attributes to the container
