---
title: DatePicker
description: 带日历弹窗的日期选择组件。
tableOfContents: false
---

import {
  BasicDemo,
  ControlledDemo,
  DefaultValueDemo,
  FormatDemo,
  DisabledDatesDemo,
  RangeDemo,
  SizesDemo,
  DisabledDemo,
} from '../../../../components/DatePickerDemo'
import DatePickerDemoSource from '../../../../components/DatePickerDemo.tsx?raw'
import Example from '../../../../components/Example.astro'

带日历弹窗的日期选择组件。

## 导入

```tsx
import { DatePicker } from 'asterui'
```

## 示例

<div class="examples">

<Example title="基础日期选择器" description="简单的日期选择。" source={DatePickerDemoSource} fn="BasicDemo">
  <BasicDemo client:load slot="demo" />
</Example>

<Example title="受控模式" description="带 onChange 处理器的受控日期选择器。" source={DatePickerDemoSource} fn="ControlledDemo">
  <ControlledDemo client:load slot="demo" />
</Example>

<Example title="默认值" description="带初始值的日期选择器。" source={DatePickerDemoSource} fn="DefaultValueDemo">
  <DefaultValueDemo client:load slot="demo" />
</Example>

<Example title="日期格式" description="不同的显示格式字符串。" source={DatePickerDemoSource} fn="FormatDemo">
  <FormatDemo client:load slot="demo" />
</Example>

<Example title="禁用日期" description="使用回调禁用指定日期。" source={DatePickerDemoSource} fn="DisabledDatesDemo">
  <DisabledDatesDemo client:load slot="demo" />
</Example>

<Example title="日期范围" description="选择开始和结束日期。" source={DatePickerDemoSource} fn="RangeDemo">
  <RangeDemo client:load slot="demo" />
</Example>

<Example title="尺寸" description="不同的输入框尺寸。" source={DatePickerDemoSource} fn="SizesDemo">
  <SizesDemo client:load slot="demo" />
</Example>

<Example title="禁用" description="禁用的日期选择器。" source={DatePickerDemoSource} fn="DisabledDemo">
  <DisabledDemo client:load slot="demo" />
</Example>

</div>

## API

### `DatePicker`

| 属性 | 描述 | 类型 | 默认值 |
|----------|-------------|------|---------|
| `value` | 受控日期值 | `Date \| null` | - |
| `defaultValue` | 初始日期值 | `Date \| null` | - |
| `onChange` | 变化处理器 | `(date: Date \| null) => void` | - |
| `format` | 显示格式字符串 | `string` | `'MM/DD/YYYY'` |
| `placeholder` | 输入框占位符 | `string` | `'选择日期'` |
| `disabledDate` | 禁用指定日期 | `(date: Date) => boolean` | - |
| `disabled` | 禁用选择器 | `boolean` | `false` |
| `size` | 输入框尺寸 | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl'` | `componentSize` |
| `data-testid` | 用于测试的测试 ID | `string` | - |
| `className` | 额外的 CSS 类名 | `string` | - |

### `DatePicker.Range`

| 属性 | 描述 | 类型 | 默认值 |
|----------|-------------|------|---------|
| `value` | 受控日期范围值 | `[Date \| null, Date \| null]` | - |
| `defaultValue` | 初始日期范围值 | `[Date \| null, Date \| null]` | - |
| `onChange` | 变化处理器 | `(range: [Date \| null, Date \| null]) => void` | - |
| `format` | 显示格式字符串 | `string` | `'MM/DD/YYYY'` |
| `placeholder` | 输入框占位符 | `[string, string] \| string` | `['开始日期', '结束日期']` |
| `disabledDate` | 禁用指定日期 | `(date: Date) => boolean` | - |
| `disabled` | 禁用选择器 | `boolean` | `false` |
| `size` | 输入框尺寸 | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl'` | `componentSize` |
| `data-testid` | 用于测试的测试 ID | `string` | - |
| `className` | 额外的 CSS 类名 | `string` | - |
