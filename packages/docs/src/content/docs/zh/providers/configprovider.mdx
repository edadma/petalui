---
title: ConfigProvider 全局配置
description: 用于语言环境、组件大小、方向和其他设置的全局配置提供者。
tableOfContents: false
---

import {
  BasicDemo,
  SizeDemo,
  LocaleDemo,
  DirectionDemo,
  NestedDemo,
} from '../../../../components/ConfigProviderDemo'
import ConfigProviderDemoSource from '../../../../components/ConfigProviderDemo.tsx?raw'
import Example from '../../../../components/Example.astro'

用于语言环境、组件大小、方向和其他设置的全局配置提供者。

## 引入

```tsx
import { ConfigProvider } from 'asterui'
```

## 示例

<div class="examples">

<Example title="基本用法" description="为所有子组件设置全局组件大小。" source={ConfigProviderDemoSource} fn="BasicDemo">
  <BasicDemo client:load slot="demo" />
</Example>

<Example title="组件尺寸" description="在 small、middle 和 large 尺寸之间全局切换。" source={ConfigProviderDemoSource} fn="SizeDemo">
  <SizeDemo client:load slot="demo" />
</Example>

<Example title="语言环境" description="自定义国际化的语言环境字符串。" source={ConfigProviderDemoSource} fn="LocaleDemo">
  <LocaleDemo client:load slot="demo" />
</Example>

<Example title="方向" description="支持 LTR 和 RTL 布局。" source={ConfigProviderDemoSource} fn="DirectionDemo">
  <DirectionDemo client:load slot="demo" />
</Example>

<Example title="嵌套提供者" description="嵌套的 ConfigProviders 继承并覆盖父设置。" source={ConfigProviderDemoSource} fn="NestedDemo">
  <NestedDemo client:load slot="demo" />
</Example>

</div>

## API

### `ConfigProvider`

| 属性 | 描述 | 类型 | 默认值 |
|----------|-------------|------|---------|
| `locale` | 语言环境配置对象 | `Locale` | `enUS` |
| `componentSize` | 所有组件的默认大小 | `'small' \| 'middle' \| 'large'` | `'middle'` |
| `direction` | 布局方向 | `'ltr' \| 'rtl'` | `'ltr'` |
| `prefixCls` | CSS 类的前缀 | `string` | - |
| `getPopupContainer` | 门户（模态框、下拉框）的默认容器 | `(triggerNode?: HTMLElement) => HTMLElement` | - |
| `children` | 子组件 | `React.ReactNode` | - |

## 可用的语言环境

从 `asterui/locale` 引入语言环境：

```tsx
import { zhCN, jaJP, koKR, deDE, frFR, esES, ptBR, enGB, enCA } from 'asterui/locale'
```

| 语言环境 | 语言 |
|--------|----------|
| `enUS` | 英语（美国） - 默认 |
| `enGB` | 英语（英国） |
| `enCA` | 英语（加拿大） |
| `zhCN` | 简体中文 |
| `jaJP` | 日语 |
| `koKR` | 韩语 |
| `deDE` | 德语 |
| `frFR` | 法语 |
| `esES` | 西班牙语 |
| `ptBR` | 葡萄牙语（巴西） |

## Hooks

### `useConfig`

访问当前配置值：

```tsx
import { useConfig } from 'asterui'

function MyComponent() {
  const { locale, componentSize, direction } = useConfig()
  // ...
}
```

### `useLocale`

访问当前语言环境：

```tsx
import { useLocale } from 'asterui'

function MyComponent() {
  const locale = useLocale()
  // ...
}
```

## 无障碍

- 对阿拉伯语和希伯来语等从右到左的语言使用 `direction="rtl"`
- 语言环境设置影响 Empty、Pagination 等组件中的屏幕阅读器文本
- 嵌套提供者允许应用的不同区域具有不同的配置
