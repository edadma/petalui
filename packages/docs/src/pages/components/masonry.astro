---
import ComponentLayout from '../../layouts/ComponentLayout.astro';
import Example from '../../components/Example.astro';
import {
  DynamicMasonryDemo,
  BasicMasonryDemo,
  ResponsiveMasonryDemo,
  CustomGapDemo,
} from '../../components/MasonryDemo';
import MasonryDemoSource from '../../components/MasonryDemo.tsx?raw';

const api = [
  { property: 'columns', description: 'Number of columns or responsive column configuration', type: "number | { xs?: number, sm?: number, md?: number, lg?: number, xl?: number, '2xl'?: number }", default: '3' },
  { property: 'gap', description: 'Spacing between items in pixels', type: 'number', default: '16' },
  { property: 'children', description: 'Items to display in masonry layout', type: 'React.ReactNode' },
  { property: 'className', description: 'Additional CSS classes', type: 'string' },
];
---

<ComponentLayout
  name="Masonry"
  description="Pinterest-style masonry layout for displaying items with varying heights in an organized grid."
  api={api}
>
  <Example title="Dynamic Items" description="Add and remove items dynamically. New items are placed in the shortest column." source={MasonryDemoSource} fn="DynamicMasonryDemo">
    <DynamicMasonryDemo client:load slot="demo" />
  </Example>

  <Example title="Basic Masonry" description="Simple masonry grid with varying height items." source={MasonryDemoSource} fn="BasicMasonryDemo">
    <BasicMasonryDemo client:load slot="demo" />
  </Example>

  <Example title="Responsive Columns" description="Responsive column count at different breakpoints." source={MasonryDemoSource} fn="ResponsiveMasonryDemo">
    <ResponsiveMasonryDemo client:load slot="demo" />
  </Example>

  <Example title="Custom Gap" description="Different gap sizes between items." source={MasonryDemoSource} fn="CustomGapDemo">
    <CustomGapDemo client:load slot="demo" />
  </Example>

  <section slot="accessibility" class="mb-8">
    <h2 class="text-2xl font-bold mb-4">Usage Notes</h2>
    <ul class="list-disc list-inside space-y-2 text-base-content/80">
      <li>The Masonry component creates a Pinterest-style layout where items are arranged in columns</li>
      <li>Items flow into the shortest column to create a balanced layout</li>
      <li>Use the <code class="bg-base-content/20 px-1 rounded">columns</code> prop to set a fixed number of columns or make it responsive</li>
      <li>The <code class="bg-base-content/20 px-1 rounded">gap</code> prop controls spacing between items in pixels</li>
      <li>Responsive breakpoints: xs (&lt;640px), sm (≥640px), md (≥768px), lg (≥1024px), xl (≥1280px), 2xl (≥1536px)</li>
      <li>Items can have varying heights - the layout will automatically distribute them efficiently</li>
      <li>Great for image galleries, product grids, and content cards</li>
    </ul>
  </section>
</ComponentLayout>
